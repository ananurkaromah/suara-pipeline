version: '3.8'
services:
  suara-worker:
    build: docker/
    container_name: suara_worker
    env_file:
      - .env
    volumes:
      - .:/app
      # Map the GCP key securely into the container
      - ./gcp-key.json:/app/gcp-key.json:ro